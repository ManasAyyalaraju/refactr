\documentclass[10pt,letterpaper]{article}

% Packages
%{ if resume.compact_mode %}
\usepackage[margin=0.5in,top=0.4in,bottom=0.4in]{geometry}
%{ else %}
\usepackage[margin=0.5in]{geometry}
%{ endif %}
\usepackage{enumitem}
\usepackage{hyperref}
\usepackage{titlesec}
\usepackage{fancyhdr}
\usepackage{xcolor}
\usepackage{fontenc}
\usepackage[T1]{fontenc}
\usepackage[USenglish]{babel}  % proper hyphenation patterns
\usepackage{mathptmx}  % Times New Roman font
\usepackage[stretch=25,shrink=25]{microtype}  % font expansion/protrusion for better line fills

% Hyperlink setup
\hypersetup{
    colorlinks=true,
    linkcolor=black,
    urlcolor=black,
    filecolor=black,
    citecolor=black,
    pdfborder={0 0 0}
}

% Remove page numbers
\pagestyle{empty}

% Improve text justification and line breaking for better bullet point formatting
% Allow more flexible spacing and better line filling
\tolerance=2000
\emergencystretch=3em
\hyphenpenalty=50
\exhyphenpenalty=50

% Set spacing
\setlength{\parindent}{0pt}
\setlength{\parskip}{0pt}
%{ if resume.roomy_mode %}
\setlist[itemize]{leftmargin=14pt, itemsep=2pt, parsep=1pt, topsep=0pt}
%{ else %}
\setlist[itemize]{leftmargin=14pt, itemsep=0pt, parsep=0pt, topsep=0pt}
%{ endif %}

% Section formatting
\titleformat{\section}
  {\normalfont\bfseries\uppercase}
  {}
  {0em}
  {}
  [\titlerule]

% Dynamic spacing variables based on content density. compact_mode (set
% ahead of render, from bullet/section counts) tightens spacing for dense
% resumes; roomy_mode (set by render_resume_pdf AFTER measuring that a
% one-page render still leaves the page visibly underfull) loosens it
% instead, one step past the default - so a short resume's page doesn't end
% in a blank void well before the bottom margin. Mutually exclusive - see
% render_resume_pdf's tier-stepping logic.
%{ if resume.compact_mode %}
\titlespacing*{\section}{0pt}{5pt}{1pt}
\newcommand{\entryvspace}{\vspace{1pt}}
\newcommand{\sectionvspace}{\vspace{3pt}}
\newcommand{\headervspace}{\vspace{-8pt}}
\newcommand{\contactvspace}{\vspace{2pt}}
%{ elif resume.roomy_mode %}
\titlespacing*{\section}{0pt}{18pt}{5pt}
\newcommand{\entryvspace}{\vspace{11pt}}
\newcommand{\sectionvspace}{\vspace{11pt}}
\newcommand{\headervspace}{\vspace{2pt}}
\newcommand{\contactvspace}{\vspace{9pt}}
%{ else %}
\titlespacing*{\section}{0pt}{10pt}{2pt}
\newcommand{\entryvspace}{\vspace{5pt}}
\newcommand{\sectionvspace}{\vspace{5pt}}
\newcommand{\headervspace}{\vspace{-5pt}}
\newcommand{\contactvspace}{\vspace{5pt}}
%{ endif %}

% Custom commands
\newcommand{\resumeHeader}[1]{
  \begin{center}
    {\huge\bfseries #1}
  \end{center}
  \headervspace
}

\newcommand{\resumeContact}[1]{
  \begin{center}
    #1
  \end{center}
  \contactvspace
}

\newcommand{\resumeSubheading}[4]{
  \noindent
  \begin{minipage}[t]{0.7\textwidth}
    \textbf{#1} -- \textit{#2}
  \end{minipage}
  \hfill
  \begin{minipage}[t]{0.25\textwidth}
    \raggedleft #3 -- #4
  \end{minipage}
  \vspace{1pt}
}

\newcommand{\resumeEducation}[2]{
  \noindent
  \begin{minipage}[t]{0.7\textwidth}
    \textbf{#1}
  \end{minipage}
  \hfill
  \begin{minipage}[t]{0.25\textwidth}
    \raggedleft #2
  \end{minipage}
  \vspace{1pt}
}

\newcommand{\resumeEducationSplit}[3]{
  \noindent
  \begin{minipage}[t]{0.7\textwidth}
    \textbf{#1}
  \end{minipage}
  \hfill
  \begin{minipage}[t]{0.25\textwidth}
    \raggedleft #2
  \end{minipage}
  \par\noindent #3
  \vspace{1pt}
}

\newcommand{\resumeProjectHeading}[3]{
  \noindent
  \begin{minipage}[t]{0.7\textwidth}
    \textbf{#1} -- \textit{#2}
  \end{minipage}
  \hfill
  \begin{minipage}[t]{0.25\textwidth}
    \raggedleft #3
  \end{minipage}
  \vspace{1pt}
}

\begin{document}
%{ if resume.ultra_compact_mode %}
\fontsize{9.3}{11.3}\selectfont
%{ endif %}

% Name
\resumeHeader{VAR{resume.name|escape_latex}}
\vspace{-10pt}
% Contact Information
\resumeContact{
  %{ if resume.contact %}
    %{ if resume.contact.email %}VAR{resume.contact.email|escape_latex}%{ endif %}
    %{ if resume.contact.email and resume.contact.phone %} $\cdot$ %{ endif %}
    %{ if resume.contact.phone %}VAR{resume.contact.phone|escape_latex}%{ endif %}
    %{ if (resume.contact.email or resume.contact.phone) and resume.contact.website %} $\cdot$ %{ endif %}
    %{ if resume.contact.website %}\href{VAR{resume.contact.website|ensure_url_scheme|escape_latex}}{VAR{resume.contact.website|escape_latex}}%{ endif %}
    %{ if (resume.contact.email or resume.contact.phone or resume.contact.website) and resume.contact.linkedin %} $\cdot$ %{ endif %}
    %{ if resume.contact.linkedin %}\href{VAR{resume.contact.linkedin|ensure_url_scheme|escape_latex}}{VAR{resume.contact.linkedin|escape_latex}}%{ endif %}
  %{ endif %}
}

% Headline
%{ if resume.headline %}
\begin{center}
  \textit{VAR{resume.headline|escape_latex}}
\end{center}
\vspace{5pt}
%{ endif %}

% Summary
%{ if resume.summary %}
\noindent
VAR{resume.summary|escape_latex}
\vspace{10pt}
%{ endif %}

% Education
%{ if resume.education %}
\section*{EDUCATION}
%{ for edu in resume.education %}
%# Always use two-line format: school + location on line 1, degree + major on line 2
%{ set school_text = edu.school|escape_latex %}
%{ set location_text = edu.location|escape_latex if edu.location else '' %}
%{ set major_text = edu.major|escape_latex if edu.major else '' %}
%{ set degree_text = edu.degree|escape_latex if edu.degree else '' %}
%{ set date_text = edu.graduation_date|escape_latex if edu.graduation_date else '' %}

%# Build first line: school + location (if available)
%{ if location_text %}
  %{ set first_line = school_text ~ ' -- ' ~ location_text %}
%{ else %}
  %{ set first_line = school_text %}
%{ endif %}

%# Build second line: degree + major
%{ if major_text and degree_text %}
  %{ set second_line = degree_text ~ ', ' ~ major_text %}
%{ elif degree_text %}
  %{ set second_line = degree_text %}
%{ elif major_text %}
  %{ set second_line = major_text %}
%{ else %}
  %{ set second_line = '' %}
%{ endif %}

%# Use split format only if there's a second line, otherwise use simple format
%{ if second_line %}
\resumeEducationSplit{VAR{first_line}}{VAR{date_text}}{VAR{second_line}}
%{ else %}
\resumeEducation{VAR{first_line}}{VAR{date_text}}
%{ endif %}

%{ if edu.scholarships %}
\noindent Scholarships: VAR{edu.scholarships|escape_latex}
%{ endif %}

%{ if edu.gpa %}
\noindent Overall GPA: VAR{edu.gpa|escape_latex}
%{ endif %}
%{ if not loop.last %}
\entryvspace
%{ endif %}
%{ endfor %}
%{ endif %}

% Technical Skills
%{ if resume.technical_skills %}
\section*{TECHNICAL SKILLS}
\begin{itemize}
%{ for cat in resume.technical_skills %}
  \item \textbf{VAR{cat.label|escape_latex}:} %{ for item in cat.items %}VAR{item|escape_latex}%{ if not loop.last %}, %{ endif %}%{ endfor %}
%{ endfor %}
\end{itemize}
\sectionvspace
%{ endif %}

% Experience
%{ if resume.experience %}
\section*{EXPERIENCE}
%{ for exp in resume.experience %}
\resumeSubheading{VAR{exp.company|escape_latex}%{ if exp.location %}, VAR{exp.location|escape_latex}%{ endif %}}{VAR{exp.title|escape_latex}}{%{ if exp.start_date %}VAR{exp.start_date|escape_latex}%{ endif %}}{%{ if exp.end_date %}VAR{exp.end_date|escape_latex}%{ else %}Present%{ endif %}}
%{ if exp.bullets %}
\begin{itemize}
%{ for bullet in exp.bullets %}
  \item VAR{bullet|escape_latex}
%{ endfor %}
\end{itemize}
\entryvspace
%{ endif %}
%{ endfor %}
%{ endif %}

% Projects
%{ if resume.projects %}
\section*{PROJECTS}
%{ for proj in resume.projects %}
\resumeProjectHeading{VAR{proj.name|escape_latex}}{%{ if proj.role %}VAR{proj.role|escape_latex}%{ endif %}}{%{ if proj.semester %}VAR{proj.semester|escape_latex}%{ endif %}}
%{ if proj.bullets %}
\begin{itemize}
%{ for bullet in proj.bullets %}
  \item VAR{bullet|escape_latex}
%{ endfor %}
\end{itemize}
\entryvspace
%{ endif %}
%{ endfor %}
%{ endif %}

% Leadership
%# Only show Leadership as separate section if it's substantial
%# Minor leadership (1 entry with 1 bullet) goes to Additional Info instead
%{ if resume.leadership %}
  %{ set is_minor_leadership = (resume.leadership|length == 1 and resume.leadership[0].bullets|length <= 1) %}
  %{ if not is_minor_leadership %}
\section*{LEADERSHIP}
%{ for lead in resume.leadership %}
\resumeSubheading{VAR{lead.organization|escape_latex}%{ if lead.location %}, VAR{lead.location|escape_latex}%{ endif %}}{%{ if lead.role %}VAR{lead.role|escape_latex}%{ endif %}}{%{ if lead.start_date %}VAR{lead.start_date|escape_latex}%{ endif %}}{%{ if lead.end_date %}VAR{lead.end_date|escape_latex}%{ else %}Present%{ endif %}}
%{ if lead.bullets %}
\begin{itemize}
%{ for bullet in lead.bullets %}
  \item VAR{bullet|escape_latex}
%{ endfor %}
\end{itemize}
%{ if not loop.last %}
\entryvspace
%{ endif %}
%{ endif %}
%{ endfor %}
  %{ endif %}
%{ endif %}

% Volunteer Work
%{ if resume.volunteer_work %}
\section*{VOLUNTEER WORK}
%{ for vol in resume.volunteer_work %}
\resumeSubheading{VAR{vol.organization|escape_latex}%{ if vol.location %}, VAR{vol.location|escape_latex}%{ endif %}}{%{ if vol.role %}VAR{vol.role|escape_latex}%{ endif %}}{%{ if vol.start_date %}VAR{vol.start_date|escape_latex}%{ endif %}}{%{ if vol.end_date %}VAR{vol.end_date|escape_latex}%{ else %}Present%{ endif %}}
%{ if vol.bullets %}
\begin{itemize}
%{ for bullet in vol.bullets %}
  \item VAR{bullet|escape_latex}
%{ endfor %}
\end{itemize}
\entryvspace
%{ endif %}
%{ endfor %}
%{ endif %}

% Awards
%{ if resume.awards %}
\section*{AWARDS \& HONORS}
%{ for award in resume.awards %}
\noindent
\begin{minipage}[t]{0.7\textwidth}
  \textbf{VAR{award.title|escape_latex}}%{ if award.organization %} -- \textit{VAR{award.organization|escape_latex}}%{ endif %}
\end{minipage}
\hfill
\begin{minipage}[t]{0.25\textwidth}
  \raggedleft %{ if award.date %}VAR{award.date|escape_latex}%{ endif %}
\end{minipage}
%{ if award.description %}
\par\noindent VAR{award.description|escape_latex}
%{ endif %}
\entryvspace
%{ endfor %}
%{ endif %}

% Publications
%{ if resume.publications %}
\section*{PUBLICATIONS}
%{ for pub in resume.publications %}
\noindent\textbf{VAR{pub.title|escape_latex}}
%{ if pub.authors or pub.venue or pub.date %}
\par\noindent\textit{
%{ if pub.authors %}VAR{pub.authors|escape_latex}%{ endif %}
%{ if pub.authors and pub.venue %}, %{ endif %}
%{ if pub.venue %}VAR{pub.venue|escape_latex}%{ endif %}
%{ if (pub.authors or pub.venue) and pub.date %} (%{ endif %}
%{ if pub.date %}VAR{pub.date|escape_latex}%{ endif %}
%{ if (pub.authors or pub.venue) and pub.date %})%{ endif %}
}
%{ endif %}
%{ if pub.url %}
\par\noindent\href{VAR{pub.url|ensure_url_scheme|escape_latex}}{VAR{pub.url|escape_latex}}
%{ endif %}
\entryvspace
%{ endfor %}
%{ endif %}

% Additional Information
%# Precompute exactly what this section would render, once, so the section
%# header's own guard can never disagree with what actually ends up on the
%# page below it - the previous guard (resume.additional_info or ... or
%# resume.leadership) was true whenever those objects merely EXISTED, not
%# when they had anything to show (e.g. any resume.leadership truthied the
%# header even though only the "exactly 1 entry, <=1 bullet" case ever
%# renders anything from it) - printing a bare "ADDITIONAL INFORMATION"
%# header followed by nothing.
%{ set show_skills_line = (not resume.technical_skills) and (resume.additional_info.computer_skills or resume.additional_info.technical_skills or resume.skills) %}
%{ set show_certifications = resume.additional_info and resume.additional_info.certifications %}
%{ set show_languages = resume.additional_info and resume.additional_info.languages %}
%{ set show_memberships = resume.additional_info and resume.additional_info.professional_memberships %}
%{ set show_minor_leadership = resume.leadership and (resume.leadership|length == 1 and resume.leadership[0].bullets|length <= 1) %}
%{ set show_work_eligibility = resume.additional_info and resume.additional_info.work_eligibility %}
%{ set show_other = resume.additional_info and resume.additional_info.other %}
%{ set show_additional_info = show_skills_line or show_certifications or show_languages or show_memberships or show_minor_leadership or show_work_eligibility or show_other %}

%{ if show_additional_info %}
\section*{ADDITIONAL INFORMATION}

%{ if show_skills_line %}
  %{ if resume.additional_info.computer_skills %}
\noindent\textbf{Computer Skills:} VAR{resume.additional_info.computer_skills|escape_latex}
  %{ elif resume.additional_info.technical_skills %}
\noindent\textbf{Technical Skills:} VAR{resume.additional_info.technical_skills|escape_latex}
  %{ else %}
\noindent\textbf{Skills:} %{ for skill in resume.skills %}VAR{skill|escape_latex}%{ if not loop.last %} $|$ %{ endif %}%{ endfor %}
  %{ endif %}
%{ endif %}

%{ if show_certifications %}
\par\noindent\textbf{Certifications:} %{ for cert in resume.additional_info.certifications %}VAR{cert|escape_latex}%{ if not loop.last %}, %{ endif %}%{ endfor %}
%{ endif %}
%{ if show_languages %}
\par\noindent\textbf{Languages:} %{ for lang in resume.additional_info.languages %}VAR{lang|escape_latex}%{ if not loop.last %}, %{ endif %}%{ endfor %}
%{ endif %}
%{ if show_memberships %}
\par\noindent\textbf{Professional Memberships:} %{ for mem in resume.additional_info.professional_memberships %}VAR{mem|escape_latex}%{ if not loop.last %}, %{ endif %}%{ endfor %}
%{ endif %}
%{ if show_minor_leadership %}
  %{ set lead = resume.leadership[0] %}
\par\noindent\textbf{Leadership:} VAR{lead.organization|escape_latex}%{ if lead.role %} - VAR{lead.role|escape_latex}%{ endif %}%{ if lead.start_date %} (VAR{lead.start_date|escape_latex}%{ if lead.end_date %} - VAR{lead.end_date|escape_latex}%{ else %} - Present%{ endif %})%{ endif %}
%{ endif %}
%{ if show_work_eligibility %}
\par\noindent\textbf{Work Eligibility:} VAR{resume.additional_info.work_eligibility|escape_latex}
%{ endif %}
%{ if show_other %}
\par\noindent VAR{resume.additional_info.other|escape_latex}
%{ endif %}
%{ endif %}

\end{document}

